#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX 5
int queue_arr[MAX];
int front, top;
front = -1;top = -1;
int data;
enqueue(int a){
    if(front==MAX-1){
        printf("Overflow");
    }
    else if {
        top++;
        queue_arr[top]=a;
    }
}
dequeue(int a){
    if(top==-1){
        printf("Underflow");
    }
    else{
        int value=
    }
}
peek()
isfull()
isempty()
display()
int main(){
     data=pop();
}
